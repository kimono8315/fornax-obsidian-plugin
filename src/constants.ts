// Constants for Fornax plugin
import { FornaxSettings } from './types';

export const DEFAULT_SETTINGS: FornaxSettings = {
	// No settings currently needed
};

export const VIEW_TYPE_FORNAX = 'fornax-telescope';